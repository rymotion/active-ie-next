export default class EventInformation {
  constructor(id, name, title, refURL, imgURL) {
    this.id = id;
    this.name = name;
    this.title = title;
    this.refURL = refURL;
    this.imgURL = imgURL;
  }
}
